
import 'package:flutter/material.dart';

//

iconsPath(String name) =>
    "assets/icons/${name.replaceAll(' ', '_').toLowerCase()}.svg";

const unitsFile = 'assets/data/goofy_units.json';

const appColor = Colors.indigo;
const appIcon = "assets/icons/app_icon.svg";
const appLegalese = "© Maxeem America, 2019";

const backdropHeaderSize  = 16.0;
const backdropHeaderOffset = 0.0;

